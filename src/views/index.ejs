<!DOCTYPE html>
<html lang="en">
<%- include("partials/head") %>
<body  class="<%- locals.user ? user.backgroundColor : null %>">
	<%- include("partials/header") %>

	<main id="register" >
		<% if (locals.user ) { %>
			<div class="registro-container">
				
					<div class="container-fluid w-50 form-control text-center p-4 registro-form m-4">
						<h6>Hola ​<b><%= user.name %></b>​, elegiste el color: <b><%= user.backgroundColor %></b>,<br> tu email es: <b><%= user.email %></b> ​y tu edad es: <b><%= user.edad %></b></h6>
						<a href="/user" class="btn border btn-success">Presiona el boton</a>
						<a href="/user/reset" class="btn btn-danger">Olvidar el color</a>
					</div>
				
			</div>
			<img src="/images/node.jpg" width="45%" >
			<% }else{ %>

				<div class="registro-container">

					<form action="/" method="post" >
						
						
		
						<div class="registro-form">
			
							<h4>Elija un color</h4>
						
							<div class="input-container">
								<h6 class="datos-box">Nombre*</h6>
								<input class="input-style" type="text" name="name" placeholder="Ingresar nombre..." value="<%= locals.old && old.name %>">
								<% if (locals.errors && errors.name) { %>
									<small class="text-danger"><%= errors.name.msg %> </small>
								<% } %>
							</div>
							
							<!-- color -->
		
							<div class="mb-4 ">
								<label for="select" class="form-label">Color de fondo*</label>
								<select class="form-select w-75 mx-auto" id="select" aria-label="Default select example"
								name="backgroundColor">
									<option value="azul">Azul</option>
									<option value="verde">Verde</option>
									<option value="amarillo">Amarillo</option>
									<option value="naranja">Naranja</option>
									<option value="negro">Negro</option>
		
								</select>
								<span class="text-danger small"><%= locals.errors && locals.errors.backgroundColor ? errors.backgroundColor.msg : null %> </span>
							  </div>
		
							<div class="input-container">
								<h6 class="datos-box">Email*</h6>
								<input class="input-style" type="email" name="email" placeholder="Ingresar email..."value="<%= locals.old && old.email %>">
								<% if(locals.errors && errors.email) { %>
									<small class="text-danger"><%= errors.email.msg %> </small>
									<% } %>
							</div>
							<div class="input-container">
								<h6 class="datos-box">Edad*</h6>
								<input class="input-style" type="edad" name="edad" placeholder="Ingre su edad..."value="<%= locals.old && old.edad %>">
								<% if(locals.errors && errors.edad) { %>
									<small class="text-danger"><%= errors.edad.msg %> </small>
									<% } %>
							</div>
			
							<p>Campos obligatorios *</p>
							<div class="input-check">
								<div class="form-check">
									<input class="form-check-input" type="checkbox" value="checkBox" id="flexCheckDefault" name="checkBox">
									<label class="form-check-label" for="flexCheckDefault">
									Recordar color
									</label>
								</div>
							</div>
							
							<button class="button-container" type="submit">Enviar</button>
						</div>
					</form>
				</div>

			<%}%>
        
    </main>

	<%- include("partials/footer") %>
</body>
</html>